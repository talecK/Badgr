<%= form_for [current_user, @user_achievement] do |f| %>
  <div class="flash warning"> TODO - Add comment box here </div>
  <%= hidden_field_tag 'achievement_id', @achievement.id %>
<%= f.submit "Request" %>
<% end %>

<br />
<% flash.each do |key, value| %>
  <div class="flash <%= key %>"><%= value %></div>
<% end %>

<% @user_achievement.errors.to_a.each do |error| %>
  <%= error %>
<% end %>

